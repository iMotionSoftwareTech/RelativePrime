# RelativePrime
The RelativelyPrime Java App is a program that takes an integer n and prints an nxn table. An asterisk * is printed at (i, j) if i and j are relatively prime (their greatest common divisor is 1), otherwise a space is printed.
